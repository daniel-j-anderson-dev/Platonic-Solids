#ifndef CAMERA_H
#define CAMERA_H

#pragma once

// #include "../include/Quaternion.h"

class Camera
{
public:
    Camera();
    // Camera(Quaternion position, Quaternion orientation, double viewportDistance);
    ~Camera();

    // Quaternion getPosition();
    // Quaternion getOrientation();
    // double     getViewportDist();

    // void setPosition(Quaternion position);
    // void setOrientation(Quaternion orientation);
    // void setViewportDist(double viewportDist);
    

private:

    // Quaternion position;
    // Quaternion orientation;
    // double viewportDist;

};

#endif